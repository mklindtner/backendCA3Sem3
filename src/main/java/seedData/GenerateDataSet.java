package seedData;

import javax.persistence.EntityManager;
import javax.persistence.Persistence;

public class GenerateDataSet
{
	/*
	public static void main(String[] args)
	{
		seed();
	}
	public static void seed()
	{
		User  user  = new User("p1", "email1", "123");
		//User user2 = new User("p2", "email2", "123");
		try {
		System.out.println("initalizing perisstence file");
		EntityManager em = Persistence
				.createEntityManagerFactory("REST_excersices")
				.createEntityManager();



			em.getTransaction().begin();
			em.persist(user);
			em.getTransaction().commit();
			em.close();
			System.out.println("done");
			//UserRepository userRepository = new UserRepository( em );

			//userRepository.post(user);
			//userRepository.post(user2);


			//Persistence.generateSchema("REST_excersices", null);
		} catch(Exception e) {
			throw new RuntimeException(e);
		}

	} */


}

